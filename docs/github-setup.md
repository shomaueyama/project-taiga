# GitHub Setup

The repository is designed with `main` as a protected branch.

Recommended branch protection:

- Require pull requests before merging.
- Require CI to pass.
- Disallow force pushes.
- Disallow branch deletion.
- Require conversation resolution.

Do not change repository visibility or bypass branch protection from automation.
